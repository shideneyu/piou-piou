= simple_form_for Tweet.new, remote: true do |f|
  div class="form-actions"
    = f.input :content, :label => false, :required => false, as: 'string'
    = f.button :submit, :class => 'btn btn-primary'